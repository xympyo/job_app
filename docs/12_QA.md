# V1 verification

Date: 2026-09-14. Workspace: D:\Moshe\JobApplications.

## Automated checks

- `npm run lint`: passes with no errors or warnings.
- `npm test`: 24 tests across domain/persistence, UI flows and mocked Supabase Auth.
- `npm run build`: production build succeeds; vendor chunks split. Current Zod package
  emits harmless Rollup annotation warnings. With no Supabase environment configured,
  the SDK is tree-shaken and its named chunk is empty; this is expected.
- `npm run test:db`: 10 PostgreSQL integration groups pass against the actual migration.
- `npm audit`: zero known vulnerabilities at verification time.
- `npm ci --dry-run`: lockfile install validation passes.

Business tests cover create/edit, reusable company and multiple sources, applications,
CV/vacancy snapshots, flexible stages and rejection stage, separate draft/final answers,
completion/character limits, due items, search/filtering, ready vacancies, unique companies,
freshness, malformed/oversized/unknown-key/null imports, unsafe URLs, fit explanations,
title/URL deduplication, preview/no mutation, merge/skip/keep and within-batch duplicates,
local persistence, stale saves, storage corruption/quota errors, cloud errors and CSV safety.

UI tests cover protected routes, explicit local entry, logout, job create/edit → prepare →
Applied → question persistence, recoverable database errors, preview/duplicate/confirm import,
and email/password rejection/success/session/logout with a mocked Supabase Auth service.

PostgreSQL tests create auth.users/auth.uid and anon/authenticated roles in PGlite, then
execute the actual SQL migration. They check owned CRUD, second-user isolation, denied
cross-owner insert/FKs, anonymous denial on every table/RPC, batch rollback, table whitelist,
spoofed ownership, committed RPC results, stale-write rejection, immutable snapshots,
history-preserving deletion restrictions, required answers/limits and rejection stage.
This is real SQL/RLS execution, not a text inspection of policy statements.

## Browser and responsive QA

Playwright CLI drove only the local app. No external vacancy websites were automated.
Created an explicitly labeled fictional QA vacancy, selected mode/family, saved it on
a phone viewport, opened details, prepared an application, and saved separate draft and
final answers through the browser. Test data is confined to the dedicated QA browser.
The normal workspace ships empty apart from three CV entities.

Checked dashboard at widths 320, 375, 390, 768, 1024 and 1440; vacancy details at 320, 390,
768, 1024 and 1440; research, companies and toolkit at 390, 768 and 1440; question form at
390. No horizontal overflow in these checks. Examined screenshots of desktop dashboard,
desktop/tablet/phone details, phone inbox/form and desktop research/toolkit.

Axe WCAG 2 A/AA and WCAG 2.1 AA checks: no violations in checked dashboard, detail,
question form, research, company and toolkit views. Avatar contrast was corrected.
Hidden mobile sidebar is also hidden from keyboard focus; controls have explicit labels,
described hints, visible focus, and native modal dialog focus containment.
A fresh final browser session reported zero console errors/warnings after navigating
the completed resource views. HMR errors during active component/dependency replacement
were not counted as final runtime results.

Production preview with no environment keys shows configuration instructions, no local
mode button, and no access to protected workspace routes. That fail-closed behavior was
checked in the browser separately from development local mode.

## Evidence

Screenshots in `output/playwright` include:

- dashboard-desktop.png
- inbox-phone.png
- detail-390.png, detail-768.png, detail-1440.png
- application-desktop.png, question-phone.png
- career-toolkit-desktop.png, research-import-desktop.png

Populated screenshots are labeled QA examples, not discovered job opportunities.

## Limits and release boundary

No production Supabase project or Vercel account was connected. Actual hosted Auth,
PostgREST/RPC behavior, account configuration, cross-device sync and deployment remain
to be smoke-tested using Moshe's project. Follow README connection steps, then exercise
sign-in → add job → apply → question → refresh → second device → sign-out. Verify email
before sign-in and keep deployed environment keys public, never service-role keys.

Automated accessibility checks do not substitute for every screen-reader/browser/device
combination. No claim is made of exhaustive certification or real iOS/Android testing.
Full-backup restore UI and private PDF uploads are deferred; JSON export is available.

## 2026-09-14 — Connection and credential boundary follow-up
Supabase public Auth and secret-key Admin/Data probes succeed. The project has zero Auth users and no app tables; signup remains enabled pending management access. Vercel project/GitHub linkage and domain verified; only the two public Supabase settings remain in Preview/Production. npm run check passes: 27 tests, lint and production build. Three new checks cover the configuration allowlist, privileged-key rejection and bundle leak prevention. Live persistence is not yet verified.

Production deployment a84a12f is Ready at https://job-app-nine-lake.vercel.app (HTTP 200; Career Command Center title). Five linked JS/CSS assets contain none of the supplied private credentials. All 10 PostgreSQL integration checks pass. Management API reads for Auth settings and database table inventory both returned 403 insufficient privileges with the newly supplied token; no schema or Auth mutations were attempted.

## 2026-09-14 — Production acceptance (supersedes earlier cloud limits)
- Updated token in .env reaches the intended project. Initial failure came from an inherited process token overriding the file; direct parsing resolved it.
- Applied 202609140001_initial.sql to the previously empty project using the Management API. Verified all eight tables have row security enabled. This direct application is not registered in Supabase CLI migration history; reconcile history before adopting CLI db push.
- Enabled signup with mandatory confirmation; set Auth site URL to https://job-app-nine-lake.vercel.app. Confirmed `disable_signup=false`, `mailer_autoconfirm=false`, 12-character minimum and one-hour OTP expiry through the Auth config endpoint.
- Provisioned owner and two disposable QA users with Auth Admin. No emails were sent. Real password sign-in succeeded for all three.
- Exercised actual createCloudRepository and domain commands against live Supabase: seed CVs, create company/job/source, prepare application with snapshot, move to Applied, save independent draft/final answer and create an interview record. Reload returned all records and two stage-history entries.
- Separate user could not read the first user's vacancy or spoof ownership. Anonymous reads failed on every table. Stale RPC writes were rejected.
- Hosted Playwright session signed in, viewed the saved application, edited its final answer at 390px, saved and refreshed. A second fresh browser session signed in and saw the edited answer. This checks shared persistence, not realtime updates to already-open views.
- Reviewed hosted desktop 1440x1000 and phone 390x844 screenshots (cloud-application-desktop.png and cloud-question-phone.png). Phone layout has no horizontal overflow. Both browser sessions report zero console errors/warnings. Browser sign-out returned to /login and removed private record content.
- Deleted both QA Auth accounts and verified cleanup: one owner, three CV rows, zero rows in all seven other tables. Owner password remains only in ignored local private files.
- npm run check passes: 27 tests, lint and production build. Existing local SQL suite had 10 passing integration groups. No changes to app or migration code were needed for activation.

Physical phone hardware, other browsers and exhaustive accessibility coverage remain outside this smoke test. Private PDF upload and full-backup restore are still deferred V1 limitations.

## 2026-09-14 — Signup acceptance
- Production owner password was set to the requested value and password sign-in was verified; credentials remain only in ignored local files.
- Actual Supabase signup with a Gmail alias returned an unconfirmed user with no session; a separate unconfirmed sign-in was rejected with `email_not_confirmed`. Disposable accounts were deleted afterward.
- Auth Admin generated a real confirmation action link, which redirected with Supabase's default access-token fragment to `/auth/confirm`. The app now waits for Supabase to consume that fragment and also supports PKCE `code` and token-hash links without exposing one-time credentials in browser history.
- Signup UI validates name, email, password length/confirmation, uses generic duplicate-account messaging, adds a resend flow with a cooldown, handles rate limits and expired links, and prevents duplicate submissions. New users do not inherit another user's CV seed records.
- 34 automated tests pass, including signup validation, delivery failure, resend, confirmation, expiry recovery, unsupported link handling and new-account isolation.
- Custom confirmation-template changes were rejected by this free-tier project while the default email provider is active. Default emails remain restricted/rate-limited; configure custom SMTP for general public signup delivery.
