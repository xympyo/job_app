# V1 verification

Date: 2026-09-14. Workspace: D:\Moshe\JobApplications.

## Automated checks

- `npm run lint`: passes with no errors or warnings.
- `npm test`: 24 tests across domain/persistence, UI flows and mocked Supabase Auth.
- `npm run build`: production build succeeds; vendor chunks split. Current Zod package
  emits harmless Rollup annotation warnings. With no Supabase environment configured,
  the SDK is tree-shaken and its named chunk is empty; this is expected.
- `npm run test:db`: 10 PostgreSQL integration groups pass against the actual migration.
- `npm audit`: zero known vulnerabilities at verification time.
- `npm ci --dry-run`: lockfile install validation passes.

Business tests cover create/edit, reusable company and multiple sources, applications,
CV/vacancy snapshots, flexible stages and rejection stage, separate draft/final answers,
completion/character limits, due items, search/filtering, ready vacancies, unique companies,
freshness, malformed/oversized/unknown-key/null imports, unsafe URLs, fit explanations,
title/URL deduplication, preview/no mutation, merge/skip/keep and within-batch duplicates,
local persistence, stale saves, storage corruption/quota errors, cloud errors and CSV safety.

UI tests cover protected routes, explicit local entry, logout, job create/edit → prepare →
Applied → question persistence, recoverable database errors, preview/duplicate/confirm import,
and email/password rejection/success/session/logout with a mocked Supabase Auth service.

PostgreSQL tests create auth.users/auth.uid and anon/authenticated roles in PGlite, then
execute the actual SQL migration. They check owned CRUD, second-user isolation, denied
cross-owner insert/FKs, anonymous denial on every table/RPC, batch rollback, table whitelist,
spoofed ownership, committed RPC results, stale-write rejection, immutable snapshots,
history-preserving deletion restrictions, required answers/limits and rejection stage.
This is real SQL/RLS execution, not a text inspection of policy statements.

## Browser and responsive QA

Playwright CLI drove only the local app. No external vacancy websites were automated.
Created an explicitly labeled fictional QA vacancy, selected mode/family, saved it on
a phone viewport, opened details, prepared an application, and saved separate draft and
final answers through the browser. Test data is confined to the dedicated QA browser.
The normal workspace ships empty apart from three CV entities.

Checked dashboard at widths 320, 375, 390, 768, 1024 and 1440; vacancy details at 320, 390,
768, 1024 and 1440; research, companies and toolkit at 390, 768 and 1440; question form at
390. No horizontal overflow in these checks. Examined screenshots of desktop dashboard,
desktop/tablet/phone details, phone inbox/form and desktop research/toolkit.

Axe WCAG 2 A/AA and WCAG 2.1 AA checks: no violations in checked dashboard, detail,
question form, research, company and toolkit views. Avatar contrast was corrected.
Hidden mobile sidebar is also hidden from keyboard focus; controls have explicit labels,
described hints, visible focus, and native modal dialog focus containment.
A fresh final browser session reported zero console errors/warnings after navigating
the completed resource views. HMR errors during active component/dependency replacement
were not counted as final runtime results.

Production preview with no environment keys shows configuration instructions, no local
mode button, and no access to protected workspace routes. That fail-closed behavior was
checked in the browser separately from development local mode.

## Evidence

Screenshots in `output/playwright` include:

- dashboard-desktop.png
- inbox-phone.png
- detail-390.png, detail-768.png, detail-1440.png
- application-desktop.png, question-phone.png
- career-toolkit-desktop.png, research-import-desktop.png

Populated screenshots are labeled QA examples, not discovered job opportunities.

## Limits and release boundary

No production Supabase project or Vercel account was connected. Actual hosted Auth,
PostgREST/RPC behavior, account configuration, cross-device sync and deployment remain
to be smoke-tested using Moshe's project. Follow README connection steps, then exercise
sign-in → add job → apply → question → refresh → second device → sign-out. Verify email
before sign-in and keep deployed environment keys public, never service-role keys.

Automated accessibility checks do not substitute for every screen-reader/browser/device
combination. No claim is made of exhaustive certification or real iOS/Android testing.
Full-backup restore UI and private PDF uploads are deferred; JSON export is available.

## 2026-09-14 — Connection and credential boundary follow-up
Supabase public Auth and secret-key Admin/Data probes succeed. The project has zero Auth users and no app tables; signup remains enabled pending management access. Vercel project/GitHub linkage and domain verified; only the two public Supabase settings remain in Preview/Production. npm run check passes: 27 tests, lint and production build. Three new checks cover the configuration allowlist, privileged-key rejection and bundle leak prevention. Live persistence is not yet verified.

Production deployment a84a12f is Ready at https://job-app-nine-lake.vercel.app (HTTP 200; Career Command Center title). Five linked JS/CSS assets contain none of the supplied private credentials. All 10 PostgreSQL integration checks pass. Management API reads for Auth settings and database table inventory both returned 403 insufficient privileges with the newly supplied token; no schema or Auth mutations were attempted.

## 2026-09-14 — Production acceptance (supersedes earlier cloud limits)
- Updated token in .env reaches the intended project. Initial failure came from an inherited process token overriding the file; direct parsing resolved it.
- Applied 202609140001_initial.sql to the previously empty project using the Management API. Verified all eight tables have row security enabled. This direct application is not registered in Supabase CLI migration history; reconcile history before adopting CLI db push.
- Enabled signup with mandatory confirmation; set Auth site URL to https://job-app-nine-lake.vercel.app. Confirmed `disable_signup=false`, `mailer_autoconfirm=false`, 12-character minimum and one-hour OTP expiry through the Auth config endpoint.
- Provisioned owner and two disposable QA users with Auth Admin. No emails were sent. Real password sign-in succeeded for all three.
- Exercised actual createCloudRepository and domain commands against live Supabase: seed CVs, create company/job/source, prepare application with snapshot, move to Applied, save independent draft/final answer and create an interview record. Reload returned all records and two stage-history entries.
- Separate user could not read the first user's vacancy or spoof ownership. Anonymous reads failed on every table. Stale RPC writes were rejected.
- Hosted Playwright session signed in, viewed the saved application, edited its final answer at 390px, saved and refreshed. A second fresh browser session signed in and saw the edited answer. This checks shared persistence, not realtime updates to already-open views.
- Reviewed hosted desktop 1440x1000 and phone 390x844 screenshots (cloud-application-desktop.png and cloud-question-phone.png). Phone layout has no horizontal overflow. Both browser sessions report zero console errors/warnings. Browser sign-out returned to /login and removed private record content.
- Deleted both QA Auth accounts and verified cleanup: one owner, three CV rows, zero rows in all seven other tables. Owner password remains only in ignored local private files.
- npm run check passes: 27 tests, lint and production build. Existing local SQL suite had 10 passing integration groups. No changes to app or migration code were needed for activation.

Physical phone hardware, other browsers and exhaustive accessibility coverage remain outside this smoke test. Private PDF upload and full-backup restore are still deferred V1 limitations.

## 2026-09-14 — Signup acceptance
- Production owner password was set to the requested value and password sign-in was verified; credentials remain only in ignored local files.
- Actual Supabase signup with a Gmail alias returned an unconfirmed user with no session; a separate unconfirmed sign-in was rejected with `email_not_confirmed`. Disposable accounts were deleted afterward.
- Auth Admin generated a real confirmation action link, which redirected with Supabase's default access-token fragment to `/auth/confirm`. The app now waits for Supabase to consume that fragment and also supports PKCE `code` and token-hash links without exposing one-time credentials in browser history.
- Signup UI validates name, email, password length/confirmation, uses generic duplicate-account messaging, adds a resend flow with a cooldown, handles rate limits and expired links, and prevents duplicate submissions. New users do not inherit another user's CV seed records.
- 34 automated tests pass, including signup validation, delivery failure, resend, confirmation, expiry recovery, unsupported link handling and new-account isolation.
- Custom confirmation-template changes were rejected by this free-tier project while the default email provider is active. Default emails remain restricted/rate-limited; configure custom SMTP for general public signup delivery.
- The canonical Vercel domain no longer redirects to the generated alias, so Auth site and signup redirect URLs resolve directly at `job-app-nine-lake.vercel.app`.
## 2026-09-14 — First usable career workflow acceptance

Supersedes earlier incomplete workflow/cloud boundaries above. Feature commit dd85b38
was deployed through GitHub/Vercel before this live acceptance run.

- Live owner password login and three CV records verified. All eight tables have RLS
  enabled with authenticated user ownership policies; no schema change was needed.
- Disposable account browser test: Overview Add vacancy → fictional manufacturing IT
  Business Analyst in Cikarang/Hybrid → source, description, requirements, deadline,
  date found, explained Strong Fit 82 and Analyst CV → Review → Save → Ready to Apply.
- Applications → Prepare application → Applied on 2026-09-14 with Analyst CV, notes and
  dated next action → required question with 500-character limit, independent draft/final
  answer and strategy notes → Completed → HR Interview.
- At 390x844, refreshed, edited next action, signed out (private view cleared), signed
  back in and verified saved answers/action. Recorded Rejected at HR interview with
  feedback. History retained the record after navigation and refresh.
- Phone research import rejected malformed JSON. Corrected a valid two-entry preview,
  changed location before confirmation and kept default duplicate skip. One new vacancy,
  its source and one research run persisted; the original was not duplicated.
- Live domain/repository commands additionally saved Offer and Withdrawn applications
  and interview notes. Browser History displayed all three outcomes after refresh.
  Changing current vacancy text left the original application snapshot intact.
- Second account could not read any first-account rows across eight populated tables;
  anonymous access returned no data/denial. Cross-owner application-question link failed.
  Stale writes failed; a multi-change transaction failure rolled back its earlier insert.
- Desktop/phone screenshots reviewed: workflow-desktop.png and workflow-phone.png under
  output/playwright. Phone History scroll width equals viewport width (390). Browser
  console reported zero errors/warnings at final inspection.
- Both disposable accounts and all their domain rows were deleted. Owner retained three
  CV rows and zero other domain rows. No emails, real applications or employer messages
  were sent. QA examples are fictional and were never added to the owner's workspace.
- Final npm run check passed 36 tests, lint and build after removing the redundant empty
  dashboard promotional panel. Existing SQL migration integration suite passed 10 groups.

SMTP and physical iOS/Android hardware are outside this milestone. Viewport/browser
testing demonstrates responsive behavior, not universal device certification. Offer and
withdrawal writes used live domain commands; the main rejection scenario used browser UI.

## 2026-09-14 — Production import failure investigation

- Reproduced the actual seven-role batch shape, including all fit fields, arrays, empty
  values, dates, CV recommendations, posting/work-mode values, HTTP(S) source/apply URLs,
  research metadata and mixed source labels.
- Local parsing passed with seven jobs. Production on `pyoloker.vercel.app` passed the
  same file through upload, validation and preview as “Review 7 opportunities”.
- A disposable production account confirmed the same batch. The UI reported 7 vacancies
  added, 0 skipped/merged, and 1 research run saved. Admin readback showed 7 jobs and 7
  sources for that account; deletion then removed the account and all rows.
- The failing layer represented by the reported message was schema validation of nested
  `jobs[*].sources[*].source_type`. The old bundle accepted only the five canonical enum
  values. Commit 2417762 normalizes the batch’s descriptive labels before strict parsing.
- The prior regression was only a one-job/two-source example and did not exercise the
  full batch through production confirm. It also predated the report’s stale-bundle
  observation, so it could not detect an old asset being served.
- Production asset `index-BHDA4wNa.js` contains the alias mapping and has the same SHA-256
  as the local build from commit 2417762. The latest Vercel deployment is Ready at
  `job-maxhmyhrx-moshe-dayans-projects.vercel.app`; `pyoloker.vercel.app` serves it.
- Added a seven-role realistic regression test. `npm run check` passes 38 tests; the
  database/RLS suite passes all 10 checks. No owner rows were changed; owner readback is
  three CV rows and zero other domain rows.

## 2026-09-14 — Controlled remediation

- Approved audit remediation is implemented without changing deferred areas such as
  fuzzy duplicate matching, navigation consolidation or performance optimization.
- `npm run check` passes 42 tests, lint and production build. `npm run test:db` passes
  all 10 PostgreSQL/RLS checks using both migrations.
- Added an axe-core dashboard smoke test and documented 320px responsive coverage as a
  release requirement alongside the existing 390px and desktop checks.
- Applied `202609140002_application_preparing.sql` to production through the Management
  API. It changes only application-stage semantics and constraints; owner rows were
  not modified.
- Production acceptance after deployment must cover Attention routing, Preparing/date
  guardrails, legacy Verified-open presentation, translated import errors, 320px and
  390px layouts, and cleanup of disposable users/data.

## 2026-09-14 — Controlled remediation production acceptance

- Vercel production deployment is Ready at job-375tw13uv-moshe-dayans-projects.vercel.app,
  serving commit a5154b5537e9c2d17ca163e38b722c51f484df33 through https://pyoloker.vercel.app/.
- Disposable production journey passed: research import/preview, Ready to Apply,
  Prepare (Preparing), applied-date guardrail, Applied/HR Interview, required question
  validation, completed answer, interview event, Attention routing, explicit
  Applied-to-Preparing confirmation, rejection and History retention.
- Production invalid import returned human-readable job/company/source repair guidance.
- Responsive checks passed at 390px and 320px with no horizontal overflow; the 320px
  question dialog fit within the viewport. Accessibility smoke test passed locally.
- Disposable account and all associated rows were deleted. Owner readback remained 3 CVs,
  7 jobs, 7 companies, 7 sources and 1 research run, with no owner application,
  question or event rows changed.

## 2026-09-15 — Bulk triage verification

- Added contract tests for export, strict parsing, valid updates, duplicate/unknown IDs,
  invalid decisions, unknown keys, unchanged rows, timestamp rules and rollback-before-commit.
- `npm run check` passes 46 tests, lint and production build. Existing DB/RLS suite remains
  10 checks. Accessibility smoke coverage remains passing.
- Production acceptance completed with a disposable account. Owner records remained
untouched; the exact live result is recorded below.

## 2026-09-15 — Overview/Inbox routing remediation

- Reproduced the issue: the Ready to Apply Overview card opened Applications, while
  Inbox’s default exclusion hid Ready/Skipped rows even when an explicit status view was
  intended. Triage shortcuts were the only exact decision links.
- Fixed with URL-backed `triage`, `status` and `query` filters. Ready to Apply now opens
  the Inbox status view; triage shortcuts preserve Apply ASAP vs Apply distinction.
- Domain tests cover all four triage decisions, explicit Ready/Skipped status filtering,
  and search compatibility.

## 2026-09-15 — Application list/date remediation

- Terminal application stages are excluded from the default Applications list and remain
  retrievable with explicit status filters and History; Applied remains active.
- Applied transitions automatically record today when no applied date exists. Existing
  dates, manual edits and later-stage transitions preserve the chosen date.
- Applications list counts and rows are limited to jobs with an application record;
  unprepared Ready to Apply vacancies remain in Inbox.
- Added domain/UI regression coverage. `npm test` passes 49 tests; DB/RLS coverage remains
  10 checks.
## 2026-09-15 — Jobs workspace consolidation

- Verified Jobs lifecycle filtering, application-status priority, terminal exclusion and
  legacy route redirects with 50 automated tests.
- `npm run lint`, production build and 10 PostgreSQL/RLS checks pass.

## 2026-09-15 — Start Here guide

- Guide route renders all four workflow cards and prompt library; copy feedback is
  visible and keyboard-accessible.
- Empty and established workspace next-step selection uses current records only.
- `npm run check` passes with 51 tests; no database changes were required.

## 2026-09-16 — V2 Gate 1 read-only compiler

`npm run check` passes with 62 tests, including the Gate 1 compiler tests. Coverage includes Moshe/synthetic
isolation, provider-neutral protocol content, privacy redaction, task minimisation, deterministic
hashes, manifest agreement, malformed inputs, unsafe dependency linting and adversarial user text.
`npm run v2:gate1` generates non-production samples under `output/v2-gate1-samples/` for manual
review. Compilation uses no network, database, Supabase credentials, filesystem CV paths or owner
records. External-AI execution is not claimed; generated Markdown is designed for manual copy.

### Gate 1 preset semantics

- **Private Minimum:** omits contact details, preferred name, freeform notes, account identifiers and document binaries to minimise disclosure.
- **Working Context:** adds preferred/display name and relevant freeform context while still omitting contact details, secrets, account identifiers and document binaries.
- **Full Career Context:** adds approved evidence and retained freeform notes, but still omits contact details, secrets, account identifiers and document binaries unless explicitly selected later.

## 2026-09-16 — V2 Gate 2A profile foundation

- `npm run check`: 9 test files, 67 tests, lint and production build pass.
- `npm run test:db`: 14 PostgreSQL/PGlite ownership, RLS, foreign-key, draft/publish,
  immutability, anonymous-denial and rollback checks pass.
- Domain coverage includes empty new-user isolation, one-profile/one-draft semantics,
  stable item IDs and experience types, provenance/unknown-value handling, optimistic
  concurrency, explicit discard, deterministic revision hashing, and published-profile →
  Gate 1 Career Pack equivalence.
- No production migration, browser profile UI, Moshe profile seed or owner-data mutation
  was performed. Gate 2B onboarding remains deferred.

## 2026-09-16 — V2 Gate 2B QA

Focused Career UX tests cover: new-user Home setup CTA, guided profile creation with target direction and experience, explicit publish, local persistence, edit-through-copied-draft, changed-field review, discard confirmation, skip path and axe accessibility smoke. The editor uses responsive CSS breakpoints covering desktop and narrow phone layouts (including 320px/390px constraints); no production behavior was tested or changed because Gate 2B is local-only. Database/RLS behavior remains covered by the existing Gate 2A suite.
