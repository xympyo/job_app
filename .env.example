VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
# Local development automatically permits explicit local mode when both keys are absent.
# Production builds require Supabase. Never place a service-role key here.
